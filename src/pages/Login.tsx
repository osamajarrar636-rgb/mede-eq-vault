import { FormEvent,useState } from 'react'; import { Navigate,useNavigate } from 'react-router-dom'; import { KeyRound,Mail,ShieldCheck } from 'lucide-react'; import { supabase } from '../lib/supabase'; import { useAuth } from '../hooks/useAuth';
export default function Login(){const {user}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [mode,setMode]=useState<'login'|'reset'>('login');const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');const [err,setErr]=useState('');if(user)return <Navigate to="/" replace/>;
 const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setErr('');setMsg('');try{if(mode==='reset'){const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/login`});if(error)throw error;setMsg('Password reset instructions sent.');}else{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;nav('/');}}catch(e:any){setErr(e.message||'Authentication failed.')}finally{setBusy(false)}};
 return <div className="login-page"><div className="login-brand"><div className="brand-mark big">M</div><div><h1>MedEq Vault</h1><p>Private Medical Equipment Knowledge Base</p></div></div><div className="login-card"><div className="login-icon"><ShieldCheck/></div><h2>{mode==='login'?'Secure sign in':'Reset password'}</h2><p className="muted">Authorized biomedical engineering workspace</p><form onSubmit={submit}>{<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="engineer@organization.com" required/></label>}{mode==='login'&&<label>Password<div className="input-icon"><KeyRound size={17}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" required/></div></label>}{err&&<div className="alert error-bg">{err}</div>}{msg&&<div className="alert success-bg">{msg}</div>}<button className="primary full" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Send reset email'}</button></form><button className="text-btn" onClick={()=>{setMode(mode==='login'?'reset':'login');setErr('');setMsg('')}}>{mode==='login'?'Forgot password?':'Back to sign in'}</button></div><div className="login-footer"><span><ShieldCheck size={14}/> Private & encrypted</span><span><Mail size={14}/> Authenticated access only</span></div></div>}
